# 接手说明

给接着往下优化这个站点的人。打包日期 2026-09-11。

先读这三份：本文件（怎么接手、有哪些坑）、`README.md`（每个部分怎么运作）、`交付说明.md`（这一版交付了什么、和需求文档怎么对应）。`本地查看.md` 是给不写代码的同事看的。

---

## 1. 跑起来

需要 Node 22 和 pnpm。

```bash
pnpm install
pnpm dev        # http://localhost:8081/ ，改 src/ 下任何文件自动重建刷新
pnpm build      # 生成 _site/
```

中文在 `/`，英文在 `/en/`。

## 2. 三条红线

**一、这个压缩包里的 git 仓库没有 remote。** 是故意去掉的。原仓库的 `origin` 指向 `phai-labs/phai-labs.github.io`，也就是**正在对外服务的官网**；一次误 push 或误跑部署脚本就会把线上页面顶掉。要接回去请先跟 PhAI Labs 确认。

**二、`pnpm deploy`（`scripts/deploy.mjs`）会覆盖线上。** 它以 `HIDE_DRAFTS=1` 构建，然后清空 `main` 分支的工作树、把产物拷进去、提交并推送。这是发布动作，不是构建动作，别顺手跑。

**三、当前分支是 `rebuild`，从未推送过。** 48 个提交的历史都在，`git log` 能看到每一步为什么这么改。

## 3. 目录速览

```
src/_data/        所有文案与配置（JSON）。zh / en 是两个独立字段，各自用本语言写，不要互译
  site.json         外部链接、发布门控 releases、主视觉兜底图
  copy/*.json       各页面文案
  i18n.json         导航、按钮等界面词
  team.json         团队成员
  news/*.json       新闻与文章
  diagrams.json     示意图的主张与属性
src/pages/        每个页面一个 .njk，用 pagination 同时产出中英两份
src/_includes/    layouts 与 components
src/assets/
  css/site.css      主样式；landing.css 只服务 /landing/
  js/orb.js         主视觉引擎（见下）
  js/hero-orb.js    首页 / 合作页的初始化
  js/landing-orb.js /landing/ 的初始化
  js/field.js       全站背景的计算场
  js/site.js        揭示动画、进度条、抽屉导航等
scripts/deploy.mjs  发布（见红线二）
```

## 4. 最近这一版改了什么

主视觉从"静态图 + 循环视频"换成了**实时绘制的 canvas**（`src/assets/js/orb.js`）：暗色球体外面一圈发光的环，环的远侧从球后穿过、近侧从球前穿过，纵深就来自这层遮挡关系。首页、合作页、`/landing/` 三处共用这一个引擎，各自传不同参数。

参数表、实现要点和几个"改之前先看一眼"的注意事项，都写在 `README.md` 的「首页主视觉」一节。这里只强调最容易再踩的一个：

> **鼠标监听必须挂在 `window` 上，不能挂画布的父元素。** `.hero__kv` / `.stage__kv` 是 `pointer-events:none`（为了让上面的文案可选中），挂在那里的监听器永远不会触发。坐标从画布的 `getBoundingClientRect()` 反算。

随之删掉的：`site.js` 里的视频加载器、`field.js` 里给旧插画做动效的 KV 图层、`site.json` 里的 `kv.video` 与 `kv.geometry`，以及两个视频文件。构建产物从 9.6 MB 降到 5.9 MB。需要取回视频：`git checkout b4f9c20 -- src/assets/video`（原始素材 `particle_website_background.zip` 也还在客户那边）。

验证过的范围：Chrome，1440 与 390 两个宽度，中英文，外加 `prefers-reduced-motion`，共 12 个视图，无 console 错误，软件渲染下 65–160 fps。

## 4b. 9 月 11 日这一轮又改了什么

四个提交，都在 `git log` 里，按时间倒序：

**主视觉配色（方向 B）** — `orb.js`。先修了两个真 bug：`hue` 计算里的 `band * 1.25` 会让 `band > 0.8` 的粒子超过 1.0 被 clamp 顶住，**34% 的粒子被钉死在同一张精灵图上**，十二级色阶在有效像素上的明度跨度只有 L\* 2.8；凹槽按 `u^0.8` 定位却按原始 `u` 取密度，两者错开约 26 device px。然后把色阶改成单一色相线 `#c9d3ec → #9ba7ff → #4a55b7`，星球本体从 `#151827` 提到 `#282848`——原来它和页面底色只差 1.6 个 L\*，等于一圈光环围着一个洞。

评审提过一道"黄铜刻度线"（唯一用 `source-over` 实体绘制的暖色弧），**没有采用**：它是画面上最实的一笔，横穿标题且盖过 CTA。去掉后琥珀色 CTA 是整屏唯一的暖色。

**星场分层 + 粒子单遍定位** — `orb.js`。星场从一层变三层（远/中/近，视差 3/8/16 CSS px，尺寸和颜色也分层），总数仍是 230 颗。粒子循环原来对整个数组遍历两次（远侧一次近侧一次），每次都算全部三角函数再丢掉一半；现在一次定位、按远近写进扁平缓冲区。实测 1.8ms → 1.5ms，画面无变化。

**灯光按钮** — `site.css` + `site.js`。描边按钮的 1px 边框在光标附近出现一道镜面高光，用的是星球光标辉光那组颜色。**它不加光**：亮起时撤掉原来整圈的提亮，实测边框总光量 12227 → 6607。监听挂 `window`（`.hero__kv` 是 `pointer-events:none`），媒体查询就是全部动效策略——减少动态效果和触屏下伪元素根本不存在。顺带修掉 `.modules-list li a:hover` 动 `padding-left` 的布局动画。

**闭环动效** — `landing.njk` + `landing.css`。落地页那张五节点图里，一个 token 绕环走 01→02→03→04→05→01，20 秒一圈。纯 CSS，`offset-path`，和六张示意图的 `.dg__mv` 同一写法。

---

## 5. 已知的小瑕疵

- 首页 hero 底边有一个约 2% 亮度的台阶：背景计算场在 hero 内被遮罩压暗、hero 外没有，边界处因此有极轻微的明暗差。已经把 hero 底部遮罩改成完全淡出、把台阶降到肉眼基本看不出，但没有彻底消除。彻底解法是让遮罩延伸到 hero 之外，或让计算场在 hero 区域内不做区别对待。
- `/landing/` 是一个独立的极简首页实验，`noindex`，不在主导航里，只能从首页手动改地址进入。它和主站共用字体与色板，但样式表是独立的 `landing.css`。要不要保留、要不要合并进主站，还没定。

## 6. 待客户拍板的内容问题

这些都是**内容**决策，不是技术问题，需要 PhAI Labs 的人回答后才能动：

1. 技术页 simulator 组件的箭头方向是否要翻回原样。
2. 「可训练 Verifier 信号」这一项该不该放进 Frontier AI Lab 那个框里。
3. 团队页 Yingcheng Wu 是否临床医学出身，以及他的研究方向措辞是否准确。
4. 团队页要不要加 ORCID / LinkedIn 链接。
5. 首页第 04 节是否保留。
6. ScienceBuddy / ScienceIDE / DFM / JEPA Anything 四个项目的正式对外链接（现在是 `site.json` 里 `releases` 的门控占位，`live: false`）。
7. 招聘页需要一份真实岗位列表（现在是示例）。
8. 上线时间与批准。

## 7. 发布门控怎么用

`src/_data/site.json` 的 `releases` 字段控制四个项目的链接何时可点：

```json
"releases": {
  "sciencebuddy": { "date": "2026-09-16", "live": false, "url": "..." }
}
```

`live: false` 时页面显示「9 月 16 日发布」这类待发布状态而不是可点链接。发布当天改成 `true` 并重新构建即可。新闻条目另有 `draft` 标记，`HIDE_DRAFTS=1` 构建时草稿不会生成页面、也不进列表——`pnpm deploy` 就是这么构建的，所以本地看得到的草稿不会跟着上线。
