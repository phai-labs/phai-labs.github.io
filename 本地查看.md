# 本地查看这个网站

三步，不需要安装任何东西。

## 1. 解压

解压后是一个 `phai-labs-site` 文件夹，里面的 `_site` 就是已经构建好的网页。

## 2. 起一个本地服务器

**不能双击 `index.html` 打开。** 页面里的图片、字体都是绝对路径，用 `file://` 打开会全部失效。

在 `phai-labs-site/_site` 这个目录里打开终端（Windows 可以在地址栏输入 `cmd` 回车），然后任选一条：

```bash
python -m http.server 8080          # 电脑上有 Python
npx serve .                         # 电脑上有 Node
php -S localhost:8080               # 电脑上有 PHP
```

VS Code 用户也可以在 `_site` 上点右键选 “Open with Live Server”。

## 3. 打开浏览器

- 中文：http://localhost:8080/
- 英文：http://localhost:8080/en/

---

## 首页

首页是极简开头 + 完整站点骨架：一屏说完主张，接一张「递归发现闭环」的图（五条虚线一直在流动），
再接三条发布预告，每条都能点进新闻，约 2.7 屏。顶部导航和页脚保持完整。

原来那版信息完整的五段式首页已经移除，备份在 `D:\PhAILab\老首页备份-20260911\`，随时可以取回。

鼠标移到描边按钮上时，边框会在光标附近亮起一小段，值得停几秒看看。

---

## 看的时候需要知道的几件事

**首页主视觉是实时画出来的**，不是图片也不是视频：一个暗色球体外面一圈发光的环，环的远侧从球后穿过、近侧从球前穿过。把鼠标移到环上，附近的粒子会被点亮并被吸过来，整圈跟着提亮；点一下，一道脉冲会沿着环跑一圈。

系统开了「减少动态效果」时只画一帧静止的画面，样子一样，不动；关掉 JavaScript 才会退回一张静态图。

**新闻里有四篇是草稿。** 9 月 15 至 18 日那四篇标着未来日期的文章目前是可见的，方便审阅。正式发布用的构建会自动隐藏它们，页面上也不会生成。

**各项目的链接大多是「9 月 x 日发布」状态。** 技术报告、GitHub、产品入口这些还没公开，所以显示为待发布的样子，不是坏链接。发布当天在配置里改一个开关就会变成真链接。

**中文标题用的是自带的思源宋体子集**，所以任何电脑上看到的字形都一样，不会退化成宋体。

---

## 想看看代码或者改内容

需要 Node 22 和 pnpm，在 `phai-labs-site` 根目录：

```bash
pnpm install
pnpm dev        # http://localhost:8081/ ，改了文件自动刷新
pnpm build      # 重新生成 _site
```

所有文案都在 `src/_data/` 下的 JSON 里，页面模板只负责排版。详见根目录的 `README.md` 和 `交付说明.md`。
