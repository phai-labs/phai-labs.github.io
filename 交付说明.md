# PhAI Labs 官网 · 第一阶段重构 · 交付说明

定稿日期：2026-09-10。源码在本地仓库的 `rebuild` 分支，尚未推送到 GitHub；上线时用 `pnpm deploy` 把构建产物推到 `main` 分支（见 README「部署」）。

本版相对 9 月 7 日那版的改动：按《PhAI官网调整建议》完成全部 15 项修改（新主视觉与官方 logo、品牌名大小写、三个工作按更新版 brief 改名、首页合作计划引导与技术发布选取规则、合作页 GALILEO 真实案例、去掉申请截止时间、统一申请表链接）；主视觉改为实时绘制的 canvas 引擎（见第五节）；团队页三位创始人的简介按本人主页逐条核实修正，其中「尹榛菲」的名字此前写错为「尹臻非」。

---

## 一、如何在本地展示

### 方式 A：直接看已经构建好的页面（不需要安装任何东西）

压缩包里的 `_site/` 就是构建产物（静态 HTML）。因为页面里的资源路径是 `/assets/...` 这种绝对路径，**不能双击 index.html 用 file:// 打开**，要用一个本地静态服务器：

```bash
cd _site
python -m http.server 8080        # 有 Python 的话
```

然后浏览器打开 http://localhost:8080/ （中文）和 http://localhost:8080/en/ （英文）。

没有 Python 也可以用 Node：`npx serve _site`，或者 VS Code 的 Live Server 插件。

### 方式 B：开发模式（改内容后自动刷新）

需要 Node 22 和 pnpm。

```bash
pnpm install
pnpm dev          # 启动 http://localhost:8081/
pnpm build        # 生成 _site/
```

`pnpm dev` 启动后改任何 `src/` 下的文件都会自动重建并刷新浏览器。

### 注意

- 压缩包里的 `_site` **显示了 9 月 15–18 日的草稿新闻**，方便审阅。正式上线用 `pnpm deploy`：它以 `HIDE_DRAFTS=1` 构建并把产物推到 GitHub 的 `main` 分支，草稿页面不会生成，也不会出现在列表里。
- 各项目的产品入口、项目主页、论文、代码链接目前都显示为"9 月 16 日发布"这类待发布状态，发布当天在 `src/_data/site.json` 里把对应项目的 `live` 改为 `true` 并重新构建即可（见第四节）。
- 首页、合作页和 `/landing/` 的主视觉是**实时画出来的**（canvas），不是图片也不是视频，打开即动，没有加载等待。鼠标移到光环上会有粒子响应，点击会有一道脉冲绕环跑一圈。
- 全站按 `prefers-reduced-motion` 自动降级：系统里开了「减少动态效果」的用户看到的是完全静态的版本。
- 必须用 HTTP 服务器打开，不能双击 HTML 文件。字体和脚本在 `file://` 下都会失效。

---

## 二、实现了什么，对应需求文档的哪一部分

需求文档：《PhAI Labs 官网第一阶段重构需求》；PR Brief：《PhAI Labs 9月15日技术发布 PR Brief (1)》。

### 需求文档 §一 网站定位 / §二 信息架构

| 需求 | 实现 |
|---|---|
| 顶部导航 Tech / News / About / Team / Careers / Collaborate / 中英切换 | 六个页面全部实现，导航顺序一致，当前页有 active 状态；右上角一键切换语言，切换后停留在同一页面的对应语言版本 |
| 默认中文，中英文独立字段维护 | 中文在 `/`，英文在 `/en/`，由同一份数据生成；所有文案都是 `{"zh": …, "en": …}` 两个字段，不是临时翻译 |
| 投资者关系预留结构 | `/investors/` 占位页，含"投资者信息 / 公告与新闻 / 公司治理 / 投资者联系方式"四个预留栏目，页脚有入口，不含任何融资信息 |

### 需求文档 §三 整体视觉方向

| 需求 | 实现 |
|---|---|
| 深色近黑背景，PhAI 蓝 / 紫蓝主色，琥珀只用于关键点和按钮 | 背景 #0a0a0b；主色蓝 #9ba7ff 与紫蓝；琥珀只出现在"立即申请"按钮、已上线状态点、节点线的关键节点、每张示意图唯一的一个关键点 |
| 网格、粒子、流线、渐变、光晕 | 全站底层有一层缓慢漂移的蓝紫"计算场"和呼吸的光点（`field.js`）；首页与合作页的 KV 图上叠加了沿线流动的信号和呼吸的节点；章节间有刻度网格与光晕 |
| 编辑杂志式布局，减少卡片和方框 | 各页面以大标题、留白、横向关系组织内容；列表用细线分隔，不用方框；只有"最新动态"提示条和合作计划的探索案例保留了极细的边框 |
| 中文大标题用高对比衬线，正文无衬线，可靠的中文回退 | 标题：自带一份思源宋体子集（约 100 KB，只含标题里出现的汉字），Windows 不会退化成 SimSun；正文：苹方 / Noto Sans SC / 微软雅黑（微软雅黑做了字号校正） |
| 首页主 KV：底色与网页一致、无黑色矩形边缘、向左延伸到标题下方、有呼吸感、支持静态图和动态版、移动端重新裁切 | 用确认的网络粒子图；径向 + 纵向遮罩融进背景；图向左延伸到标题下方并降低透明度；桌面端进入视野后无缝切换为 24 帧动态 WebP（已弃用 2.7 MB 的 GIF）；手机端改为"上半屏图 + 下方文字"，不压缩变形 |

### 需求文档 §四 全局交互要求

| 需求 | 实现 |
|---|---|
| 完整的 scroll reveal（序号、眉题、标题、正文、图片、模块、章节过渡） | 所有标题、段落、列表项、图片进入视野时上浮显现，同一章节内依次错开；章节顶部的细线进入视野时从左向右画出；节点线画完后有光点沿线跑到关键节点 |
| 淡入、上移、图片渐显、线条和节点延展、章节连续过渡 | 均已实现；页面切换用 View Transitions 交叉淡入（不支持的浏览器直接跳转） |
| 顶部进度条与滚动绑定 | 已实现，精确反映位置 |
| 导航 active 状态 | 主导航按页面高亮；技术页和合作页的页内子导航随滚动高亮当前章节 |
| 支持 `prefers-reduced-motion` | 开启后所有动画停止，背景场只画一帧，示意图直接显示完成态，页面可正常浏览 |

### 需求文档 §五 响应式适配

| 需求 | 实现 |
|---|---|
| 1280×800 / 1440×900 / 1728×1117 / 1920×1080 / 移动端 | 全部用 `clamp()` 和响应式容器；12 个页面在 500 / 1280 / 1440 / 1728 / 1920 五个宽度下做了横向溢出检查，全部无溢出 |
| 大标题不盖内容、不多余换行、中英标题合理断行 | 中英文各有独立的标题宽度上限；中文标题按字断行且标点不出现在行首 |
| 序号、眉题、导航文字可读性 | 眉题、序号、子导航字号和对比度已提高 |

### 需求文档 §六 各界面要求

**首页**：Logo、主 KV、主标题（含蓝色关键词）与副标题、品牌说明、"立即申请"（跳飞书问卷）、"了解 DFM"、最新技术发布入口；KV 下方是"DFM 发现闭环"节点线；随后依次为 DFM 章节（含技术报告 / GitHub 两行，链接上线前显示为待发布）、三个独立工作（带发布日期）、PhAI 三个支柱、合作计划、最新三条新闻。

**技术页**：按需求的结构——1. PhAI 的技术方向；2. DFM（是什么、如何连接科学问题 / 模型训练 / 工具调用 / 真实实验 / 外部证据、六项能力、技术报告与 GitHub 入口）；3–5. ScienceBuddy、ScienceIDE、JEPA Anything 各一节（介绍、要点、使用链接 / 技术报告 / GitHub 或论文 / 代码，按发布日期门控）；另有"未来可能的组合路径"（按 Brief 标注为未来方向）和"逐步实现、持续发布"的路线与四日发布日历。每个工作配一张说明它在做什么的动图（见下）。

**新闻页**：时间轴式编辑列表，头条大图 + 其余条目带节点；每篇支持中英标题、发布时间、摘要、封面图（未提供封面时自动用该工作的示意图）、正文、外部链接、相关推荐；首页展示最新三条。已写好 9 月 15 日 DFM 发布、9 月 15 日合作计划、9 月 16 日 ScienceBuddy、9 月 17 日 ScienceIDE、9 月 18 日 JEPA Anything 五篇草稿。

**关于我们**：PhAI Labs 是什么 / 为什么关注真实科学发现 / 从模型能力到科学问题与知识生产 / DFM 在技术路线中的位置 / 长期愿景与价值观 五章，加发展节点时间线；与团队页内容不重复。

**团队**：保留"科学定义 → 模型训练 → 真实执行 → 持续改进"的视觉主线；三位创始人的照片、姓名、职位（CEO / President / CSO，按需求文档参考图）、简介、研究方向、个人主页与 Google Scholar 链接。

**加入我们**：为什么加入 PhAI / 当前研究方向 / 适合的人 / 工作方式 / 职位列表 / 投递方式与邮箱；职位是开放式列表，在 `src/_data/jobs.json` 里加一条即可，不改页面代码。**注意：这页的文案是我写的，不出自文档，需要你们审核或替换（见第三节）。**

**合作**：完整承接 DFM 科学家合作计划手册的内容（为什么发起 / 什么是 DFM / 什么样的问题 / 合作对象 / 合作方式 / 探索案例 / 推进流程 / 常见问题 / 申请），页内子导航，"立即申请"跳飞书问卷；三个工作的展示卡按日期显示状态；新增"两边各带什么来"的示意图，整张图可点击进入申请表。

### 需求文档 §七 内容维护要求

| 需求 | 实现 |
|---|---|
| 文章、团队、职位、外部链接结构化维护 | 分别在 `src/_data/news/*.json`、`team.json`、`jobs.json`、`site.json` |
| 中英分开管理 | 每条数据都有 `zh` / `en` 字段 |
| 主视觉可调 | 不是图片，是 `src/assets/js/orb.js` 里的 canvas 引擎，参数见 README「首页主视觉」 |
| 新闻可新增、编辑、下线 | 加 JSON 文件即新增；`draft: true` 即不上线 |
| 组件化，不把文案写死在页面 | 所有文案在 `src/_data/` 下的 JSON，页面模板只负责排版 |
| 外部链接集中管理 | 全部在 `site.json` 的 `links` 与 `releases` |
| SEO、Open Graph、分享卡片 | 每页有 title / description / canonical / hreflang / OG 标签 |
| 图片 alt 文本、懒加载 | 已实现 |

### 需求文档 §八 交付要求

首页、技术、新闻、关于、团队、加入我们、合作、中英切换、响应式、进度条、全页 scroll reveal、飞书问卷跳转、KV 静态 / 动态支持、Investor Relations 预留——全部完成。

### PR Brief 的对应

| Brief 要求 | 实现 |
|---|---|
| DFM 是"围绕 Discovery Intelligence 的研究范式、能力定义和系统组织方式"，不是框架、不是完成的模型 | 全站措辞已按此改写，DFM 章节引用 Brief 原文；不出现"框架 / 蓝图 / 模块"字样 |
| ScienceBuddy、ScienceIDE、JEPA Anything 是三个完全独立的工作，不是 DFM 的子模块，无依赖关系 | 技术页、首页、合作页均如此表述；三个工作的介绍按 Brief 对应段落改写 |
| "反馈—环境—预测"只是未来可能的组合路径 | 技术页单独一节，正文与图注都标明"未来可能的研究路径，不代表已经集成" |
| JEPA Anything 不表述为已能模拟所有科学环境 | 文案与示意图都标注"目前还在探索中" |
| 四日发布节奏：15 日 DFM 与合作计划，16 日 ScienceBuddy，17 日 ScienceIDE，18 日 JEPA Anything | 首页模块列表按日期排序，技术页有发布日历，新闻五篇按日期，各处状态标签显示对应日期 |
| 提前存在的链接标"即将发布"，链接在对应日期露出 | `site.json` 的 `releases` 开关：`live` 为 false 时全站显示"9 月 x 日发布"，不出现链接 |
| 不涉及融资信息；不公开未授权案例 | 投资者页仅占位；合作页的探索案例是手册里的占位内容 |

### 每个工作的示意图（额外做的）

参考 VoiceMem 项目页"一张图就是这项工作"的做法，为五个工作各画了一张动图（进入视野时线条自己画出来，之后有光点沿线移动，鼠标悬停高亮对应部分），画面都是不需要专业背景就能读懂的：DFM 是一圈圈变大的循环；ScienceBuddy 是一段对话；ScienceIDE 是代码库变成一排工作台；JEPA Anything 是"先预测，再行动"的分岔；合作计划是两边各带什么来、在中间一起验证。用在新闻头条、文章头图和技术页各项目段。

---

## 三、文案来源与待确认事项

**来源**：约六成句子可以逐句回溯到三份文档（需求文档、PR Brief、合作计划手册），合作页 97% 是手册原文，四篇发布新闻 76–85% 来自 Brief。以下部分是我写的，需要你们审核：

1. **加入我们**整页（为什么加入、研究方向、适合的人、工作方式）和三个占位岗位。
2. **关于我们**的"为什么关注真实科学发现""从模型能力到知识生产"两章的大部分。
3. **团队**页"团队的工作脉络"四个节点的说明，以及三位创始人的简介（按各自主页和 Stanford 档案页写的）。
4. 首页副标题"第三条 Scaling Law，是科学智能"（源自旧官网的说法）。

**上线前还需要**：

- ScienceBuddy 的正式产品链接、ScienceIDE 的项目主页（Brief 里标"待更新"）；DFM 技术报告与 GitHub 链接（9 月 15 日）；JEPA 论文 / 代码 / 演示链接（9 月 18 日）。填进 `site.json` 即可。
- 真实岗位列表（`jobs.json`）。
- 上线方式：`pnpm deploy`（见 README「部署」）。源码在 `rebuild` 分支，`main` 分支只放构建产物，GitHub Pages 直接发布 `main`。

---

## 四、日常维护速查

| 要做的事 | 改哪里 |
|---|---|
| 发布日当天让链接生效 | `src/_data/site.json` → `releases.<项目>.live: true`，链接填在 `releases.<项目>.url` 或 `links.<项目>_report` 等；该篇新闻 `draft: false`；然后 `pnpm deploy` |
| 发一篇新闻 | 在 `src/_data/news/` 加一个 JSON（复制现有一篇改内容），`draft: false` 上线 |
| 改团队信息 / 换照片 | `src/_data/team.json`，照片放 `src/assets/img/team/` |
| 加职位 | `src/_data/jobs.json` 加一条 |
| 改页面文案 | `src/_data/copy/{home,tech,about,careers,investors}.json`，合作页在 `collab.json` |
| 调主视觉 | 改 `src/assets/js/orb.js` 的参数（球的位置大小、环的宽窄倾角、粒子数），见 README「首页主视觉」；`site.json` 里的 `kv.static` 只剩关掉 JS 时的兜底图 |
| 改了标题类中文文案 | 运行 `pnpm fonts` 重新生成思源宋体子集（需要本机 Python 与 fontTools） |
| 关掉动态背景 | `src/assets/css/site.css` 里 `--field-max` 改为 0 |

更详细的说明见 `README.md`。

---

## 五、同事反馈的处理与几个判断（2026-09-09）

按《PhAI官网调整建议》逐条修改后，有几处需要说明的判断：

**首页 04「DFM 科学家合作计划」保留。** 文档问「如果首页开头体现了合作计划，最下面 04 还需不需要」。判断是保留：开头新增的只是 CTA 按钮下面的一句话加一个链接，说明「立即申请」通向什么；04 才是完整介绍（做什么、面向谁、怎么参与）。两处文字不重复。如果你们希望只保留一处，删掉 04 即可，首页仍然说得通。

**首页右侧那条技术发布保留，但改成按日期自动选。** 规则是：取尚未到发布日的最近一条；都发布完之后取最新一条。所以现在显示 9 月 15 日 DFM 发布，到 16 日自动变成 ScienceBuddy，依此类推。日期在构建时判断，所以发布当天需要重新构建（`pnpm deploy` 会做）。

**主 KV 改为实时绘制。** 原来的做法是静态图打底、`particle_website_background.zip` 里的循环视频叠在上面淡入；这一版换成了自己写的 canvas 引擎（`src/assets/js/orb.js`）：暗色球体外面一圈发光的环，环的远侧从球后穿过、近侧从球前穿过。相比视频，它只有约 20 KB、任何尺寸都清晰、可以响应鼠标，构建产物也从 9.6 MB 降到 5.9 MB。原视频文件已从仓库移除（需要时 `git checkout b4f9c20 -- src/assets/video` 可取回，原始素材 `particle_website_background.zip` 也还在）。参数怎么调见 README「首页主视觉」。9 月 11 日又做了一轮配色与交互优化——星球本体改为深海军蓝、光环走单一冷色相、星场分三层深度，描边按钮加了跟随光标的边框高光，落地页的五节点闭环图里有一个 token 在持续绕环——详见 `接手说明.md` 第 4b 节。

素材是 1280×720 原生分辨率，比静态图（1672×941）低一档，在 1920 宽的屏幕上会略软。压缩包里附了渲染脚本，如果需要更高分辨率可以重渲，但脚本里的粒子轨迹坐标是按 1280×720 写死的，要逐个换算，工作量不小，建议先看实际效果再决定。

**Logo 是官方 4c 版的反白衍生版。** 官方文件是白底黑字，深色背景直接用会看不见，所以做了反白：文字转白、Labs 保持次一级灰、无限符号保留官方蓝。**建议设计确认**；若有官方反白版，替换 `src/assets/img/phai-labs-lockup.png` 即可。浏览器标签图标也换成了官方无限符号（`src/assets/img/favicon.png`）。

**「PhAI Labs」大小写。** 数据里一直是正确写法，是 CSS 给眉题等标签加了 `text-transform: uppercase`。已加 `brand` 过滤器把品牌名包起来豁免，并修正了合作页两处会被大写的标题和示意图图注。新增带品牌名的标签时，记得加 `| brand | safe`。

